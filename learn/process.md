# 学习进度跟踪

> 这个文件由 Claude 和你共同维护。每次开始/结束一段学习，更新这里即可。
> Claude 在新会话开始时会先读这个文件来恢复上下文。

## 学习者画像（创建于 2026-05-12）

- 后端：Node 不算熟练，**没用过 Redis 和关系型数据库**
- 前端：熟练 React + TypeScript
- 学习目的：**学 AI 网关的设计思路**
- 教程颗粒度偏好：**大而深（每节 1–2 小时）**
- 讲解风格约定：
  - 不重复教前端
  - Go 语法用 Node 类比讲解
  - Redis/DB 概念给"基础补课"
  - 代码引用使用 `file:line` 格式

## 当前进度

**正在学的章节**：第 1 节（已发布）
**下一步**：完成第 1 节的动手任务

## 章节状态

| # | 章节 | 内容状态 | 学习状态 |
|---|---|---|---|
| 01 | 项目总览与本地跑通 | ✅ 已写完整 | ⬜ 学习中 |
| 02 | Go 速成（给 Node 开发者） | ⏳ 仅提纲 | ⬜ 未开始 |
| 03 | 数据层：GORM + 多 DB + Redis | ⏳ 仅提纲 | ⬜ 未开始 |
| 04 | 认证、中间件与请求生命周期 | ⏳ 仅提纲 | ⬜ 未开始 |
| 05 | Relay 核心：多 Provider 适配 | ⏳ 仅提纲 | ⬜ 未开始 |
| 06 | 流式响应与 Token 计费 | ⏳ 仅提纲 | ⬜ 未开始 |
| 07 | 限流、分发与渠道管理 | ⏳ 仅提纲 | ⬜ 未开始 |
| 08 | 二次开发：新增一个 Channel | ⏳ 仅提纲 | ⬜ 未开始 |

状态图例：⬜ 未开始 · 🔄 进行中 · ✅ 已完成

## 第 1 节动手任务清单

- [ ] 跑通本地后端 + 前端，能登录管理后台
- [ ] 在管理后台创建一个渠道和一个 token
- [ ] 用 curl 调一次 `/v1/chat/completions`
- [ ] 按"第一次代码走读"清单读完 7 个文件
- [ ] 写 2–3 句你对"AI 网关核心职责"的理解（写在下面"学习笔记"区）

## 学习笔记 / 我的理解

> 在这里记录你的思考、疑问、卡壳点。Claude 看到这里会针对性补充。

（空）

## 卡壳点 / 待解决问题

（空）

## 修正记录

- 2026-05-12：第 1 节"步骤 2"原写"启动 MySQL + Redis"有误，已修正为 PostgreSQL + Redis，并补充"项目代码默认是 SQLite，dev-compose 显式选了 PG"的说明（`model/main.go:118` chooseDB 函数判定逻辑）
- 2026-05-12：第 1 节漏写"启动后端前必须先 `make build-all-frontends`"——`main.go:38-48` 用 `//go:embed` 把前端 dist 编进二进制，dist 不存在会直接编译报错 `no matching files found`。已新增步骤 3 解释 `//go:embed` 机制并梳理"方法 B"前后端分离工作流（3 终端：dev-api / go run / dev-web），原步骤 5/6 顺延为 6/7

## 操作约定（给 Claude）

下次会话开始时：

1. **先读这个文件**，识别 `正在学的章节` 和 `下一步`
2. **询问用户**："要继续 [当前章节] 吗？" 或 "上次有什么问题想先解决？"
3. 用户确认后：
   - 如果当前章节是"⏳ 仅提纲"状态：基于 `learn/0X-xxx.md` 的大纲展开完整内容，写入同一个文件
   - 如果是"✅ 已写完整"状态：检查动手任务进度，针对疑问展开讲解
4. **每完成一节**：更新本文件的"章节状态"和"正在学的章节"
5. **用户问问题时**：先用 `Read`/`Grep` 看实际代码再回答，**不要凭空说**
6. **每节讲解结束**：在本文件追加一节"本节学到了什么"摘要（不超过 5 条）
